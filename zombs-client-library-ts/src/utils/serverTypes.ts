export interface Server {
    fallbackPort: number,
    hostname: string,
    id: string,
    ipAddress: string,
    name: string,
    port: number,
    region: string,
    selected: boolean
  }